/**
 * Homepage showcase — product handles in display order.
 *
 * Edit this list to control which pieces appear in the “From the showroom”
 * section. When Shopify is live, use Shopify product handles; until then mock
 * handles follow `preview-{collection}-{style}`.
 *
 * @type {readonly string[]}
 */
export const HOME_FEATURED_PRODUCT_HANDLES = [
  "preview-solitaire-rings-classic",
  "preview-halo-rings-heritage",
  "preview-round-diamonds-gallery",
  "preview-necklaces-signature",
  "preview-custom-design-classic",
];
